require("babel-register");

const path = require("path");

const configPath = path.resolve(
  `${__dirname}/src/lib/sequelize/config/sequelize.config.js`
);
const modelsPath = path.resolve(`${__dirname}/src/lib/sequelize/models`);
const seedersPath = path.resolve(`${__dirname}/src/lib/sequelize/seeders`);
const migrationsPath = path.resolve(
  `${__dirname}/src/lib/sequelize/migrations`
);
module.exports = {
  config: configPath,
  "models-path": modelsPath,
  "seeders-path": seedersPath,
  "migrations-path": migrationsPath,
};
