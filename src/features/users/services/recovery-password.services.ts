export const recoveryPasswordService = async (
  category_id: string,
  category_value: string,
) => {
  // Aquí va la lógica real para recuperación de contraseña
  return { status: 200, body: { category_id, category_value } };
};
